#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void apply_permutation(char *array, int *perm, int N) {

    

    

main() {
    int i = 0;
    char array[100];
    int *N;

    scanf("%s", array);
    int len = strlen(array);

    N = (int *)malloc(sizeof(int));
    for(i = 0; i < len; i++) {
        scanf("%d", &N[i]);
    }
    void apply_permutation(array, N, len);
    printf("%s\n", array);

}
