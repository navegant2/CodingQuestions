#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void largest_subarray(int *array, int size) {

    int i;

    for (i = 0; i < size; i++) {

    }
}

main() {
    int n = 0;
    int *array;
    int i;

    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        scanf("%d", &array[i]);
    }
    largest_subarray(array, n);
}

